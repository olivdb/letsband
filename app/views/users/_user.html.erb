<li>
  <div class = 'row'>
    <div class = 'span2'>
      <%= gravatar_for user, size: 90 %>
    </div>
    <div class = 'span8'>
      <p>
      	<%= link_to user.name, user %>
        <% 
          ref_city = City.find_by_id(params[:user_city_id])
          distance_ref_city = ref_city.distance(user.city)
        %>
        <%= 'from ' + user.city.fullname + " (" + (distance_ref_city.round(1)).to_s + " km from " +ref_city.name + ")" %>
      </p>
      <p>
        <% skills = Skill.where(user_id:  user.id, instrument_id: params[:instrument_id]) %>
        <%= render partial: 'skills', object: skills %>
      </p>
    </div>
  </div>
</li>