<% provide(:title, "Contact List") %>
<% provide(:menu, "contacts") %>
<%= will_paginate %>
<div class="messages">
	<ul class="nav2">
		<%= render :partial => "contact", :collection => @users, :as => :contact %>
	</ul>
</div>
<%= will_paginate %>