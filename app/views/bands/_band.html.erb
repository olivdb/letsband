
      <div class="result_user">
<div class="avatar"><img style="border:1px solid #BDBFBF;">
      <%= image_tag band.image.url(:thumb), class: 'avatar_size avatar_position' %>
    </div>



      <h1><%= link_to band.name, band %></h1><div class="text"><%= image_tag "img/home.png", alt:"", class:'ico'  %> Playing near <strong><%= band.city.fullname %></strong>
        <% 
          ref_city = City.find_by_id(params[:band_city_id])
          if band.city.id != ref_city.id 
            distance_ref_city = ref_city.distance(band.city)
        %>
          (<%= (distance_ref_city.round(1)).to_s %> km from <%= ref_city.name %>)
        <% 
          end 
        %></div>
        <div class="text">
           <%= image_tag "img/sex.png", alt:"", class:'ico'  %> <%= band.members.count.to_s %> actif members</div>
        <% band.memberships.sort{ |a,b| a.instrument.name.downcase <=> b.instrument.name.downcase }.each do |membership| %>
          <% if membership.role != 'invited' && membership.role != 'open' %>
              
          <% end %>
        <% end %>
               <div class="text"><%= image_tag "img/music.png", alt:"", class:'ico' %> Genre : <%= band.genre.name %></strong></div>

    </div>


  